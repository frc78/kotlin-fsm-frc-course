package course.l8t1

import frc.stubs.geometry.Rotation2d
import frc.stubs.geometry.Translation2d

fun distanceBetween(a: Translation2d, b: Translation2d): Double {
    // TODO: return a.getDistance(b)
    TODO()
}

fun headingFromTo(start: Translation2d, end: Translation2d): Rotation2d {
    // TODO: subtract and take the angle.
    //   (end - start).getAngle()
    TODO()
}

fun rotatePoint(point: Translation2d, by: Rotation2d): Translation2d {
    // TODO: point.rotateBy(by)
    TODO()
}
