# Check: Pose Math

Your camera reports a game piece **2 m dead ahead** in the robot's body
frame, `Translation2d(2.0, 0.0)`. The pose estimator says the robot is at
`(4.0, 3.0)` facing **90°** (counterclockwise positive, so the robot's nose
points along field +Y).

This is the `gamePieceFieldPosition` problem from task 2. Where is the game
piece in the field frame?
