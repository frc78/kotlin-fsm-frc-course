type: choice
is_multiple_choice: false
options:
  - text: VoltageOut(6.0). The configured gains scale whatever voltage you send.
    is_correct: false
  - text: DutyCycleOut(0.5). The gains convert the duty-cycle fraction into a velocity
      target.
    is_correct: false
  - text: VelocityVoltage(50.0). The controller adjusts voltage until it reaches the
      target.
    is_correct: true
  - text: "Any of them. Once applied, the gains shape every request the motor receives."
    is_correct: false
message_correct: "Closed-loop requests like VelocityVoltage and PositionVoltage engage\
  \ the configured gains, and slot 0 is the default slot. Open-loop requests ignore\
  \ the gains."
message_incorrect: Look back at task 4. Two of these requests are open-loop and one
  is closed-loop. Which is which?
custom_name: "Check: closed loop and Slot0"
local_check: true
