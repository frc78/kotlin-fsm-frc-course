package course.l6t2

import frc.stubs.swerve.*

object Drive {
    val drivetrain = SwerveDrivetrain()

    fun teleopDrive(vx: Double, vy: Double, omega: Double, robotRelative: Boolean) {
        // TODO: if robotRelative, apply RobotCentric; otherwise FieldCentric.
        //
        // Use the same with*-builder shape:
        //   FieldCentric().withVelocityX(vx).withVelocityY(vy).withRotationalRate(omega)
        //   RobotCentric().withVelocityX(vx).withVelocityY(vy).withRotationalRate(omega)
        TODO()
    }
}
