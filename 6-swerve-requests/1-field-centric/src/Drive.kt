package course.l6t1

import frc.stubs.swerve.*

object Drive {
    val drivetrain = SwerveDrivetrain()

    fun teleopDrive(vx: Double, vy: Double, omega: Double) {
        // TODO: build a FieldCentric request and apply it to drivetrain.
        //
        //   FieldCentric()
        //       .withVelocityX(vx)
        //       .withVelocityY(vy)
        //       .withRotationalRate(omega)
        //
        // Then: drivetrain.setControl(theRequest)
        TODO()
    }
}
