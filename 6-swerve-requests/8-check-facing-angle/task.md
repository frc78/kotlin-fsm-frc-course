# Check: Facing Angle

The driver holds the "lock heading" trigger, and your FSM from task 6 switches
the drivetrain to `FieldCentricFacingAngle`, targeting the speaker. The driver
keeps working both sticks the whole time, translating across the field while
the robot stays aimed.

While this request is active, which of the driver's inputs does it override?
