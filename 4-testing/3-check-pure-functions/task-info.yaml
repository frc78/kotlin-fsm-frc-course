type: choice
custom_name: "Check: pure transition functions"
is_multiple_choice: false
options:
  - text: "It runs faster than periodic(), so the test suite finishes sooner."
    is_correct: false
  - text: "Pure functions can be tested without JUnit or any test framework."
    is_correct: false
  - text: "Given the same current state and inputs, it always returns the same next state — no hardware, no singleton state — so the test just calls it and asserts on the result."
    is_correct: true
  - text: "Pure functions cannot contain bugs, so testing them is just a formality."
    is_correct: false
message_correct: "Determinism is the whole win: the same (state, input) pair always maps to the same next state, so a test is one assertEquals — no setup, no reset(), no hardware stubs running."
message_incorrect: "Re-read the opening of task 1 (Testing Pure FSM Logic): it lists exactly what made testing through a subsystem object and periodic() painful. Which of those frictions does a pure function actually remove?"
local_check: true
